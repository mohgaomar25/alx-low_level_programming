#include "main.h"

/**
 * main - check the code.
 * _islower - function that checks for lowercase character
 * c: character checker
 * Return: 1 if c is lowercase,else 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
